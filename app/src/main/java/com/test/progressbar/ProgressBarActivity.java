package com.test.progressbar;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.ProgressBar;

import com.test.fupengpeng.testapp.R;

public class ProgressBarActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_progress_bar);
        ProgressBar progressBar001 = (ProgressBar) findViewById(R.id.progress_bar001);
        ProgressBar progressBar002 = (ProgressBar) findViewById(R.id.progress_bar002);
        ProgressBar progressBar003 = (ProgressBar) findViewById(R.id.progress_bar003);
        ProgressBar progressBar004 = (ProgressBar) findViewById(R.id.progress_bar004);
        ProgressBar progressBar005 = (ProgressBar) findViewById(R.id.progress_bar005);
        ProgressBar progressBar006 = (ProgressBar) findViewById(R.id.progress_bar006);
        ProgressBar progressBar007 = (ProgressBar) findViewById(R.id.progress_bar007);
        ProgressBar progressBar008 = (ProgressBar) findViewById(R.id.progress_bar008);
        ProgressBar progressBar009 = (ProgressBar) findViewById(R.id.progress_bar009);
        ProgressBar progressBar010 = (ProgressBar) findViewById(R.id.progress_bar010);
        ProgressBar progressBar011 = (ProgressBar) findViewById(R.id.progress_bar011);







    }
}
